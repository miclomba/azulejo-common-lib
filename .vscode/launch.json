{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Test Executable",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/out/src/test_filesystem_adapters/test_filesystem_adapters", 
            "args": [],  // Add command-line arguments if needed
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",  // Set the working directory
            "environment": [],
            "externalConsole": false,  // Set to true to use an external terminal
            "MIMode": "lldb"
        }
    ]
}